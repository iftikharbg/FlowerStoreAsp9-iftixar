@model List<Product>

@if (Model.Count == 0)
{
 <li>
     No Result
 </li>
}
else
{
    foreach (var item in Model)

    {
        <li class="d-flex">
            <div>
                
                @Html.Raw(item.Image)
                <p>@item.Name</p>
            </div>
        </li>
    }
}